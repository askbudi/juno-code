/**
 * Version information for juno-task-ts
 */

// This will be replaced by the build process
export const version = __VERSION__ || '2.0.1';

// Development flag
export const isDevelopment = __DEV__ || false;
